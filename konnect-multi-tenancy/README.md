# Konnect Multi Tenancy demo

### Usage:
This Terraform script uses the following variables that should be set:
1. `region` is the Konnect region to deploy resources in.
2. `pat` is your Konnect Personal Access Token (PAT).
3. `project` defines the project code that will be used for all resources
