# Konnect Token Rotation with Terraform

This demonstrates token rotation with Terraform using the `time_rotating` resource.

The output should be captured and put in a vault to be used by pipelines.
